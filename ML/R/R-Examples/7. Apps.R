# Use the Shiny app framework to develop a simple interactive web app in R
library(shiny)

# The code all lives in the predict directory
setwd("predict")

# This starts the Shiny web server and launches the app in the default browser
runApp(".")